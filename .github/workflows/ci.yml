name: ci - API

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  cypress-API-tests-ubuntu:
    runs-on: ubuntu-latest
    container: cypress/browsers:node12.18.3-chrome89-ff86
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run end-to-end tests on desktop viewport 🧪
        uses: cypress-io/github-action@v2
        with:
          command: npm test   